// TEMPORARY placeholder from the OpenCode-bots live check (V2/V3). Untracked, never
// commit. Deletion was blocked by the safety gate; delete this file.
import { it } from "vite-plus/test";

it.skip("V2 live OpenCode smoke test (run once on 2026-09-15; see HANDOFF-opencode-bots.md)", () => {});
